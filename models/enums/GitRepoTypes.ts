/**
 * @enum
 * @name GitRepoTypes
 * @description The type of Git Repository the project is using.
 * @author J. Trpka
 */
export enum GitRepoTypes {
  GITHUB = 'GitHub',
  GITLAB = 'GitLab',
  BIT_BUCKET = 'Bit Bucket'
}