import { Box, Heading } from "@chakra-ui/react"

const MainHeader = () => {
    return (
        <Box as="header">
            <Heading as="h1">Hello, World!</Heading>
        </Box>
    );
};

export default MainHeader;