/**
 * @enum 
 * @name ProjectType
 * @description The type of project
 * @author J. Trpka
 * @todo Will need to be heavily modified
 */
export enum ProjectType {
  FRONTEND = 'frontend',
  BACKEND = 'backend',
  FULLSTACK = 'fullstack'
}