/**
 * @function ProjectDetailHero
 * @summary Project detail hero component
 * @description The hero component for the project detail page that may show title, repo stats, and other data.
 * @author J. Trpka
 * @returns { JSX }
 */
const ProjectDetailHero = () => {
    return (
        <div></div>
    );
};

export default ProjectDetailHero;